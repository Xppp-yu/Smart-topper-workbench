{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "experiment_v0.1.schema.json",
  "title": "Experiment Runner Config v0.1",
  "description": "Declarative contract for a governed experiment config. The runtime validates the same fields with a stdlib-only check in src/topper_perception/experiments/contracts.py (no jsonschema dependency).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "exp_id",
    "task_id",
    "scope",
    "runner_type",
    "seed",
    "output_root",
    "parameters"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "experiment-v0.1"
    },
    "exp_id": {
      "type": "string",
      "pattern": "^EXP-[A-Za-z0-9][A-Za-z0-9._-]*$",
      "description": "Immutable experiment identifier; also the per-EXP-ID artifact directory name."
    },
    "task_id": {
      "type": "string",
      "minLength": 1,
      "description": "Development task that produced this experiment."
    },
    "scope": {
      "type": "string",
      "enum": ["smoke", "mini", "full"],
      "description": "smoke is cheap and deterministic; mini/full require a clean Git worktree before QUEUED."
    },
    "runner_type": {
      "type": "string",
      "enum": ["dummy", "popu_neural", "popu_neural_mini", "popu_neural_full"],
      "description": "Registered runner function. 'dummy' is the deterministic no-data runner; 'popu_neural' runs the P5.2-A2 neural smoke; 'popu_neural_mini' runs P5.2-B Mini; 'popu_neural_full' runs the governed P5.2-C repeated subject-grouped Full comparison."
    },
    "seed": {
      "type": "integer",
      "description": "Random seed; same config + seed must reproduce identical metrics."
    },
    "output_root": {
      "type": "string",
      "minLength": 1,
      "description": "Base directory under which outputs/experiments/<EXP-ID>/ is created."
    },
    "data_manifests": {
      "type": "array",
      "description": "Optional external data files pinned by content hash. The runner verifies each file's SHA-256 before QUEUED and records the verified path/hash/size into manifest.json.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "sha256"],
        "properties": {
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Path to the data manifest (absolute or relative to the repo root)."
          },
          "sha256": {
            "type": "string",
            "pattern": "^[0-9a-fA-F]{64}$",
            "description": "Expected lowercase-hex SHA-256 of the file contents."
          }
        }
      }
    },
    "parameters": {
      "type": "object",
      "description": "Runner-specific free-form parameters (e.g. n_samples, fail)."
    }
  }
}
